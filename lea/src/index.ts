/**
 * Léa — Tuteur IA incarné. Point d'entrée du monolithe modulaire (D8).
 *
 * Phase 0 : ce paquet n'exporte que des CONTRATS (aucune implémentation).
 * Les phases suivantes (P1 tranche verticale, P2 BKT/decay, P3 présence,
 * P4 échelle) brancheront des implémentations derrière ces interfaces.
 */
export * as contracts from './contracts/index.js';
